# Build artifacts / temp dirs (standalone distributable package; out-of-tree files stay out of git)
build/
cmake-build-*/
dist/
*.egg-info/
__pycache__/
*.so
*.o

# Vendored third-party references (each with its own .git); never committed
3rdparty/

# IDE / pytest caches
.idea/
.pytest_cache/

# SDD session scratch
.superpowers/

# Generated by setup.py at build time
python/fast_ulysses/_build_meta.py
benchmark-results/
COMMIT

# Measurement logs and working notes: kept locally, never committed. Source comments state the
# problem; the numbers behind a decision live here.
notes/
